# Antiphon 1.0.0 release readiness

User direction: resume the parked work, close the gaps, and prepare the next
release as **1.0.0**, not 0.6.0. Codex implements; Claude independently gates.
This is preparation, not permission to push, tag or publish. Main remains
untouched until an integration decision.

## Integrated inputs

- [x] Lifecycle: `95dd045071fd8c46e23a7b52aea676e75d914113`, Claude PASS,
  including the repeated concurrent-suite experiment that closed the blocker.
- [x] Claude discovery/census: `ed6f932677fc5c1bb071d57f789956f1aee8f1b5`,
  independently reviewed diff; merged into the release worktree.
- [x] Task schema: `5c77564fd3fbf8d90efc267a606e6a5be0dfd34c`, implemented;
  its two unprotected test branches were closed and independently re-gated
  in `641c867`; the final code gate is PASS.
- [x] Combined baseline: Apple Python 3.9, 1,578 tests, 3 named skips, OK.

## Remaining release gate

- [x] Finish independent Task 3 gate; resolve findings before final review.
- [x] Persist failing CI suite stdout/stderr without changing exit semantics.
  Real workflow-shell tests preserve exit 23/29/31 and both output streams;
  20 CI tests pass. No fatal JavaScript hooks were added.
- [x] Improve quick start, legacy upgrade and exact-id recovery instructions;
  show affected task IDs for unknown liveness in doctor.
- [x] Set package/lock to 1.0.0; ship concise release notes.
- [x] Verify generated setup rules in an isolated project. The real project's
  AGENTS.md/CLAUDE.md are ignored local output, not tracked release files.
  Claude measured 5,436/5,450 and 5,939/5,950 bytes respectively for the rule
  constants; channel instructions are 3,627/3,700. Preserve these small budgets.
- [x] Run production-scope host-wrapper census; inspect unknown categories.
  99 Claude/372 Codex production candidates; 466 excluded Claude files;
  no malformed/refused files or unknown prompt-source values. Two measured
  host metadata kinds (5 edit notifications, 2 skill injections) now use one
  shared, closed predicate. Mixed/unknown input remains visible; real-file
  tests prove hidden host records still advance the anchored source frontier.
- [x] Dependency audit: found `qs` 6.15.3 advisories; updated only that
  transitive lock entry to 6.16.0. Fresh `npm audit --omit=dev --json` reports
  zero known vulnerabilities. This is not a claim of universal security.
- [x] Run integrated Python, Node and CI/static gates: fresh `npm ci`, Python
  3.9 1,582 OK (3 skips), Node full suite exit 0; Python 3.14 1,581 OK
  (1 skip) before the one added frontier test, which passed separately.
  CI 20 OK; compile/syntax and diff-check clean. The final independent reviewer
  also ran both Python versions at 1,582 tests and standalone Node on `641c867`.
- [x] Run fresh-user E2E against immutable candidate bytes with real hosts;
  preserve output, distinguish model/network limitations from product faults.
  `641c867`: 112 passed, 0 failed, exit 0. Host configuration was preserved.
- [x] Obtain final exact-SHA independent code gate; record tarball hash and precise
  remaining limitations. Do not label an untested boundary verified.
  Claude PASS on `641c867`; 11 mutation gates reproduced, prior byte-identical
  evidence carried separately. Concurrent load control and measured round
  both passed (212 worker tests); no orphan supervisor/guardian processes.
  See [the final evidence record](2026-09-05-release-1.0.0-review.md).

Closure changes only CHANGELOG and docs. Its final tarball requires the
reviewer's installed-package smoke and doc-only byte comparison, not another
model E2E run: all executed bytes remain identical to `641c867`.

## Release boundary

Working branch: `codex/release-1.0.0`, isolated under `.worktrees/release-1.0.0`.
Main at start: `aff2f9e`; last-observed remote main: `8ae3692`.
No new workflow-manager subsystem: prioritize reliable delivery, truthful
results, safe lifecycle handling, clear onboarding and diagnosable failures.
Do not silently reset cursors, trust, live hooks, sockets or other sessions.
Do not force-reclaim an uncertain worker or discard possible result evidence.

The 420-second task outer deadline is 300 seconds maximum caller wait +
30 seconds stop-intent patience + 60 seconds diff generation + 30 seconds
startup/reconciliation margin. Cross-runtime tests bind the actual consumer.

Registry boundary checked read-only: npm latest is 0.5.0 and lookup of 1.0.0
returns E404; remote main is still `8ae369230365c856ee48a9628321c64cf71b54d2`,
with no remote v1.0.0 tag. These observations do not authorize publication.

Task 3 re-gate detail: the earlier N6 label was corrected by reading the real
consumer. Its mutation removed `outcome_unknown` from **result**'s null-exit
guard, not from cancel's state set. A bare unknown result with numeric exit
now exercises that guard without another malformed field masking it. The
same correctly versioned unknown record succeeds at status and refuses cancel.
Hop tests cover 2**53, -1, string and exponent spellings, with 2**53-1 accepted.
