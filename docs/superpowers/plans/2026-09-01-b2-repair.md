# B2 Repair Implementation Plan

> Executable. One red→green cycle per task. The red is **run and its failure
> observed** before any production edit in that task. Every production behaviour
> lands in the task whose red covers it. No push, merge, version, publish, or
> live cursor/registry/config/socket state.

**Spec:** `docs/superpowers/specs/2026-09-01-b2-repair-design.md`
**Base:** `d89d983` · **Worktree:** `.worktrees/p0-claude-identity` (one writer)

## Commands

Every `-k` below is a single unittest pattern — `unittest` has no boolean
argv, so each task's tests share one distinctive token.

| purpose | command |
| --- | --- |
| focused Python | `/usr/bin/python3 -m unittest discover -s test -k <token>` |
| focused Node | `node --test test/channel.test.mjs` |
| full | `npm test` |
| statics | `git diff --check` ; `/usr/bin/python3 -m py_compile lib/*.py` ; `node --check lib/channel.mjs` ; `bash -n test/e2e/fresh-user.sh` |
| gate | `test/e2e/fresh-user.sh` on the exact clean commit |

## Interfaces, and the task that introduces each

| interface | file | task |
| --- | --- | --- |
| `identity_proof_path(cwd, owner_key)` | `lib/peers.py` | 1 |
| `write_identity_proof(cwd, owner_key, session_id, digest)` | `lib/peers.py` | 1 |
| `rotate_identity_proof(cwd, owner_key, session_id, digest)` — one lock: capture prior, write, withdraw same-owner stale halves, bounded GC window; returns prior and current | `lib/peers.py` | 3 |
| `read_identity_proof(cwd, owner_key)` | `lib/peers.py` | 1 |
| `identity_proofs(cwd)` — read-only validated inventory | `lib/peers.py` | 1 |
| `automatic_ready(cwd, kind, peer, proof)` | `lib/antiphon.py` | 2 |
| `readIdentityProof` / `automaticReady` | `lib/channel.mjs` | 5 narrow, 6 full |
| proof reclamation sweep inside `write_identity_proof` | `lib/peers.py` | 11 |

---

### Task 1 — the proof record and its inventory · token `identity_proof`

- [ ] Red: `test_an_identity_proof_round_trips_under_the_registry_lock`;
      `test_every_malformed_identity_proof_reads_as_no_proof` — non-int and
      bool version, wrong kind, missing and non-canonical session id, malformed
      identity digest, non-canonical owner key, **filename not the digest of
      the stored owner**, identity digest not derived from the stored session
      id, torn record, empty file;
      `test_the_identity_proof_inventory_is_read_only_and_validated`.
- [ ] Run `/usr/bin/python3 -m unittest discover -s test -k identity_proof`
      → expect failure: the functions do not exist.
- [ ] Green: the four functions and the record of spec §3. No `written_at`.
- [ ] Run the same command → green. Run `-k peers` → green.
- [ ] Commit.

### Task 2 — the three-way predicate, wired · token `automatic_ready`

- [ ] Red: `test_automatic_ready_requires_endpoint_session_and_proof`;
      `test_the_resolver_status_and_doctor_share_one_readiness_predicate`;
      `test_only_a_positively_live_owner_renders_as_ready`
      (unknown and dead owners never look live).
- [ ] Run `-k automatic_ready` → expect failure.
- [ ] Green: `automatic_ready` in `lib/antiphon.py`, called by the resolver,
      `status` and `doctor`. No second copy of the rule anywhere.
- [ ] Run `-k automatic_ready`, then `-k resolve`, then `-k doctor` → green.
- [ ] Commit.

### Task 3 — the hook commits and withdraws · token `hook_identity`

- [ ] Red: `test_hook_identity_commit_precedes_any_join_decision`;
      `test_hook_identity_is_never_written_for_explicit_or_invalid_names`;
      `test_hook_identity_withdraws_only_same_owner_stale_halves`;
      `test_hook_identity_sends_at_most_one_bounded_wakeup`;
      `test_hook_identity_survives_a_failed_wakeup`.
- [ ] Run `-k hook_identity` → expect failure.
      `test_hook_identity_concurrent_rotations_leave_one_current_proof`
      — two real hooks, B and C, run concurrently through the public hook path;
      assert the final proof is C, C's session half survives, A's and B's stale
      halves are gone, each hook sent at most one wakeup, and nothing
      cross-owner or explicit was deleted.
- [ ] Green: `rotate_identity_proof` as a single locked transaction — capture
      prior, atomic replace, withdraw same-owner stale halves against the new
      current proof, bounded GC window — called once by the hook. Never
      assembled from helpers that each take the non-reentrant registry lock.
      One 250 ms wakeup to the previous current alias only, errors swallowed.
- [ ] Run `-k hook_identity`, then `-k hook` → green.
- [ ] Commit.

### Task 4 — startup lifecycle · token `automatic_registration`

- [ ] Red: `test_a_first_automatic_endpoint_registers_unready_without_a_proof`;
      `test_an_automatic_registration_must_match_an_existing_proof`;
      `test_an_automatic_reassert_requires_the_current_proof`;
      `test_a_stale_listener_cannot_republish_after_a_new_proof`.
- [ ] Run `node --test test/channel.test.mjs` → expect failure.
- [ ] Green: UNREADY candidate on first registration; register and reassert
      both gated on the matching current proof.
- [ ] Run focused Node → green.
- [ ] Commit.

### Task 5 — delivery linearizes at the listener · token `stale_inbound`

Integration, not units: the real hook, the real Python resolver, a real Node
channel.

- [ ] Red: `test_a_stale_inbound_is_refused_as_no_peer`;
      `test_a_stale_inbound_emits_zero_channel_notifications`;
      `test_a_stale_listener_flushes_its_response_then_self_retires`;
      `test_python_preserves_the_no_peer_class_for_a_stale_reply`.
      The race: resolve A → hook B commits → wakeup withheld → connect to A.
- [ ] Run `node --test test/channel.test.mjs`, then
      `/usr/bin/python3 -m unittest discover -s test -k stale_inbound`
      → expect failure: A still delivers.
- [ ] Green: Node validates the proof on every inbound delivery before
      emitting — a **narrow** reader, only what delivery needs; `ok:false` with
      `refusal_class="no-peer"`; response flushed; listener alone unregisters
      its pid-owned endpoint and closes only its channel socket; Python keeps
      the class instead of recasting it as transport.
- [ ] Run both commands → green.
- [ ] Commit.

### Task 6 — parity over the whole predicate · token `readiness_parity`

Task 5 left Node's reader narrow on purpose, so this red is real rather than
ceremonial: the full-predicate fixtures diverge until Node is extended.

- [ ] Red: `test_python_and_node_agree_on_every_readiness_fixture` — the spec
      §5 table: proof missing, wrong version, wrong kind, malformed digest,
      non-canonical id, torn, empty, naming a different session; session half
      missing or torn; endpoint missing or torn; owner, digest, pid and address
      mismatches across the three halves.
- [ ] Run `-k readiness_parity`, then `node --test test/channel.test.mjs`
      → expect divergence.
- [ ] Green: extend Node to the full predicate until every verdict matches.
- [ ] Run both → green.
- [ ] Commit.

### Task 7 — configured names · token `configured_name`

- [ ] Red, real Codex hook with a positively held writer lock:
      `test_configured_name_absent_empty_and_whitespace_may_observe`;
      `test_configured_name_uppercase_normalizes_and_joins`;
      `test_configured_name_invalid_creates_no_observation_or_route`;
      `test_configured_name_invalid_withdraws_its_own_legacy_observation`
      — seed an old observation plus a positive lock, run the real hook, then
      resolve and read `status` from a process with **no** invalid env and
      prove no projection and no route; the empty/whitespace control keeps it;
      `test_configured_name_invalid_skips_the_node_probe`.
- [ ] Run `-k configured_name`, then `node --test test/channel.test.mjs`
      → expect failure.
- [ ] Green: raw-presence predicate separate from normalized parsing; durable
      idempotent withdrawal of the hook's own canonical session id only, with
      no path or id rendered.
- [ ] Run both → green.
- [ ] Commit.

### Task 8 — the bounded probe · token `identity_probe`

- [ ] Red: `test_identity_probe_bounds_a_flooding_stdout_child`;
      `test_identity_probe_bounds_a_flooding_stderr_child`;
      `test_identity_probe_kills_and_reaps_a_child_past_the_deadline`;
      `test_identity_probe_resolves_a_child_under_the_ceiling`;
      `test_identity_probe_signals_only_the_child_when_isolation_fails`.
      The first four run with isolation established and assert prompt return,
      child **and descendants** dead and reaped, captured pipe bytes within the
      literal ceilings of spec §8, none of the captured bytes anywhere in the
      result, and `None` identity where the probe failed.
      `..._signals_only_the_child_when_isolation_fails` is deliberately
      different: it asserts the child is signalled and that no signal was
      directed at a group, and it asserts **nothing** about descendants,
      because spec §8 does not promise their death in that case.
- [ ] Run `-k identity_probe` → expect failure: `capture_output` is unbounded.
- [ ] Green: streaming reads, the literal bounds, strict decode,
      `start_new_session=True` before any group-directed signal,
      terminate → kill → reap.
- [ ] Run `-k identity_probe` → green.
- [ ] Commit.

### Task 9 — privacy · token `identity_privacy`

- [ ] Red: route-bearing failures through reply, Stop, startup, `status`,
      `doctor` and queue, each carrying a mixed-case unanchored UUID, a full
      identity digest, a raw owner key, or an automatic route —
      `test_identity_privacy_redacts_every_private_shape`;
      `test_identity_privacy_preserves_alias_and_remedy`;
      `test_identity_privacy_doctor_queue_reports_an_aggregate`;
      `test_identity_privacy_every_written_surface_agrees`.
- [ ] Run `-k identity_privacy` → expect failure.
- [ ] Green: one central redactor per language applied **before** truncation,
      `refusal_class` preserved; the thread-id prefix replaced by an aggregate;
      README, BACKLOG, `CLAUDE_RULE`, `AGENTS_RULE` and channel instructions
      updated together.
- [ ] Run `-k identity_privacy` → green.
- [ ] Commit.

### Task 10 — the reconnect cost, said out loud · token `reconnect_notice`

- [ ] Red: `test_reconnect_notice_names_the_alias_and_remedy_after_rotation`;
      `test_reconnect_notice_mixed_version_record_fails_closed`.
- [ ] Run `-k reconnect_notice` → expect failure.
- [ ] Green: the wording in all five surfaces; proofless legacy automatic
      records fail closed and are never rewritten or guessed.
- [ ] Run `-k reconnect_notice` → green.
- [ ] Commit.

### Task 11 — the window keeps its evidence, and reclamation has a caller · token `proof_lifecycle`

Reclamation is introduced **here**, in the task whose red covers it, not in
Task 1 where nothing would have exercised it.

- [ ] Red: `test_proof_lifecycle_survives_a_zero_endpoint_window`
      (hook B commits, listener A self-retires, the owner now has zero
      automatic peers; assert B's proof still exists and that `status` and
      `doctor` name B's alias with the remedy);
      `test_proof_lifecycle_reclaims_a_dead_owner_through_a_proof_write`
      — driven through the public `write_identity_proof` sweep, never by
      calling the reclaimer directly;
      `test_proof_lifecycle_read_only_surfaces_reclaim_nothing`;
      `test_proof_lifecycle_reclamation_spares_a_live_successor`.
- [ ] Run `-k proof_lifecycle` → expect failure.
      `test_proof_lifecycle_sweep_makes_progress_past_live_records`
      — more than eight leading live or unknown proofs with a dead proof
      behind them; assert it is reclaimed after a finite number of writes and
      that every live and unknown record survives;
      `test_proof_lifecycle_a_malformed_sweep_cursor_resets_to_the_start`.
- [ ] Green: a persistent sweep cursor advanced atomically under the same lock,
      each write examining the next window of at most eight and reclaiming only
      owners whose death is positively proved; a malformed cursor resets rather
      than refusing.
- [ ] Run `-k proof_lifecycle`, then `-k peers` → green.
- [ ] Commit.

### Task 12 — the exact-SHA gate

- [ ] `npm test` green; statics green; `git diff --check` clean.
- [ ] Clean commit; record the SHA.
- [ ] `test/e2e/fresh-user.sh` on that exact SHA; record the assertion count.
- [ ] Send the SHA and evidence for independent review.
- [ ] Any finding starts a new SHA and repeats both gates.
