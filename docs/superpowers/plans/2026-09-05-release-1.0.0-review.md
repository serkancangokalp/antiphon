# Antiphon 1.0.0 release evidence

## Outcome and authority

Claude's independent exact-SHA verdict is **PASS** for the code, runtime and
package at `641c867d3f874ed473bd4c2363d4158dbebcbb9c`, tree
`692b99938893fa25b971dd160ea0c315823ae9e0`. No release-blocking product finding
remained. Codex owns implementation; Claude reviewed isolated copies and ran
the real-host E2E. Neither reviewer wrote the release worktree.

The following closure changes only CHANGELOG wording and these documentation
records. It adds the literal protocol names needed to search diagnostics;
production code, package.json, lockfile, tests and CI remain unchanged.
Its installed-package recheck and documentation verdict are separate from
the full code/E2E verdict above. Do not relabel a parent-SHA E2E as a new run.

This work prepares **1.0.0**, not 0.6.0. It does not authorize integration,
push, a release tag or npm publication. Local main remains `aff2f9e`; remote
main was checked at `8ae3692`, npm latest at 0.5.0, and 1.0.0 lookup returned
E404 on 2026-09-05. No live setup, cursor reset or trust grant was performed by
the release implementation.

## Evidence

| Gate | Result |
| --- | --- |
| Fresh lock install + dependency audit | 94 packages; zero known vulnerabilities, full and production-only audit |
| Parent checkout Python 3.9.6 | 1,582 tests OK, 3 named skips |
| Parent checkout Python 3.14.7 | 1,581 OK, 1 named skip before the final frontier test; that test passed separately on 3.9 |
| Independent exact-SHA archive Python 3.9.6 / 3.14.7 | 1,582 / 1,582 OK; 10 / 8 named skips (archive lacks Git history) |
| Node protocol suite | Full parent checkout and independent standalone runs exit 0 |
| CI helper tests | 20 OK, including actual workflow-shell output and exit propagation |
| Syntax/static checks | Python compile, Node syntax and diff whitespace checks clean |
| Real-host fresh-user test | 112 passed, 0 failed, exit 0, explicitly tested `641c867` |
| Concurrent load check | Control and one measured loaded round: 212 worker tests OK; 0/1 reproductions, no orphan supervisors/guardians |
| New/rechecked mutation gates | 11/11 produced failures; original bytes restored |

The independent archives intentionally skip history-dependent tests by name;
the full-history parent checkout covers those roads. The load check used two
concurrent Python suites, not just CPU burners. Eleven earlier gates were
carried on verified unchanged code: seven Node guards and four discovery/census
guards. They are not counted as newly rerun mutations.

Task 3's remaining holes were tests, not a discovered product failure:
unsafe/negative/string/exponent hop values now fail while the largest safe
integer passes; a bare `outcome_unknown` result with numeric exit code now
reaches the actual null-exit guard. The same unknown record passes status and
refuses cancel. The earlier review's label calling the null-exit guard a
cancel guard was corrected against the code before closing it.

The new host-metadata rule uses one shared closed predicate in the reader and
census. Its ALL-to-ANY and empty-list mutations both fail. Unknown, mixed or
absent metadata stays visible under this rule, and a real-file test proves
suppressed host content advances the anchored frontier without replay.

## Artifacts

The fully exercised candidate tarball had 12 files, SHA-256:

`cfa6832099f413b838b4b48e97027cdff96aef25589a50fd9a6e562dd4a277a0`

The documentation-closure tarball also has 12 files (360,290 compressed bytes,
1,238,896 unpacked bytes), SHA-256:

`f2f7713f63c704a7454e02710b64188749ca8e93d84d0b4af14f4773b97760ba`

Local final artifact:
`/tmp/antiphon-1.0.0-final.qA38WE/antiphon-1.0.0.tgz`.
The only changed tar member is `package/CHANGELOG.md`. Repository `docs/` is
not shipped. The final handoff must name the docs closure SHA and the installed
smoke result alongside this hash.

Review logs: `/tmp/g10_*.log`; real-host evidence: `/tmp/g10_e2e.log`.
E2E retained its temporary project at
`/private/var/folders/6b/qk5_z_k12dd7hm1mw0l1wlxc0000gn/T/antiphon-e2e.pOT5nv`.
The harness verified Codex configuration unchanged and no workspace trust
granted; it deliberately retains its Codex rollout rather than deleting from
the user's session store. Logs and temporary artifacts are evidence, not
permanent storage guarantees; the commit is the reproducible source.

## Explicit limits and operator steps

- The E2E does not exercise an interactive Claude channel approval/notification,
  an open Codex session with no rollout yet, or the real Codex UserPromptSubmit
  event (the hook payload is simulated). Do not call those paths newly verified.
  After installation/restart, confirm the interactive channel through `/mcp`
  and its notification log. Claude's host channel remains a research preview.
- GitHub-hosted Linux/macOS matrix jobs are configured but were not triggered
  by this local, unpushed preparation. The runtime measurements here are macOS.
- Finish/collect old worker results before upgrading. Run setup and restart
  both hosts after installing; an old loaded server cannot acquire new
  lifecycle guarantees from updated files on disk. Linked developer installs
  need `npm link` again. No automatic live configuration rewrite is implied.
- A retained unknown worker must not be force-reclaimed or manually relabelled
  to free a slot. Preserve evidence and follow README's exact-ID recovery path.
- Doctor already prints unknown-liveness task IDs and a remedy in this SHA;
  the one-line aggregate status remains counts-only. A review note claiming
  doctor was counts-only was checked against `_doctor_workers` and its runtime
  test rather than copied into the backlog as an invented defect.
- Generated rule budgets are tight: CLAUDE_RULE 5,436/5,450 bytes (14 spare),
  AGENTS_RULE 5,939/5,950 (11 spare), channel 3,627/3,700. The next instruction
  edit must rerun the byte-budget contracts; do not silently raise the ceilings.

Useful additions chosen for this release were guided startup, explicit upgrade
and recovery steps, honest bounded results and receipt handling, a narrower
host-authorship rule, and preserved CI diagnostics. No new workflow-manager
subsystem, automatic merging or permission bypass was added.
