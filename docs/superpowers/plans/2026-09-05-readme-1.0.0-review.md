# Antiphon 1.0.0 — README presentation handoff

## Scope and release boundary

The user approved the navy, peach and mint presentation and explicitly asked
for it on top of 1.0.0 as part of the final version. This slice starts at
`66b872f11a3908a8453d0df35922a193ffa14a31`, whose tree is the previously reviewed
`ef85f62` candidate. Work is isolated on `codex/readme-1.0.0`.

The README adds an original SVG cover, product introduction, three-step setup,
an illustrative two-agent workflow and navigation. The roles are expressly
user-chosen. Transport acceptance is not presented as reading, a fresh worker
does not see uncommitted changes, and the Claude channel preview limitation is
visible in Quick start. The entire former protocol reference and every section
from Many peers onward remain byte-identical, inside/outside one new details
wrapper respectively. Existing heading targets remain available.

Supporting changes are limited to the exact cover's package allowlist and
Git ignore exception, the installed-package manifest and fixture, and four
presentation tests. `lib/`, `bin/`, `package-lock.json`, dependencies and the
version are unchanged. This is not a new runtime release or a new E2E claim.

Claude acknowledged that publication and tagging must wait for this slice's
review and integration. This worktree does not write main, remote refs, tags,
live configuration or registry state. The older `f2f7713f…` tarball is no longer
the intended presentation candidate; the earlier release review remains a
historical record of those exact bytes.

## Verification performed by Codex

- `npm ci --ignore-scripts --audit=false --fund=false`: 94 packages installed
  from the unchanged lockfile.
- `npm test`: exit 0; Python 3.9.6 ran 1,586 tests, OK (3 expected skips), then
  the full Node channel suite passed, including full-history compatibility
  paths. Full output retained at
  `/tmp/antiphon-readme-preview.17aXKu/npm-test.log`.
- README presentation tests: 4/4. They check local image existence, explicit
  packaging and alt text; self-contained SVG structure; landing-page anchor
  targets; and balanced details that leave the remaining guide visible.
- Cross-runtime/documentation contracts: 61/61. CI helper tests: 20/20.
- `git diff --check`: clean. `git diff 66b872f -- lib bin package-lock.json`:
  empty.
- Initial, unmodified baseline: one Node MCP handshake timed out at 60 seconds.
  The isolated handshake retry passed, the complete 61-test contract retry
  passed, and the final `npm test` passed. No cause was established and no
  runtime fix was attempted in this documentation slice.

## Visual verification and its limits

Rendered the actual README with a local Markdown renderer and GitHub Markdown
CSS in headless Chrome: 1280 px desktop and 390 px mobile, light and dark modes.
All four checks loaded the local SVG, found every fragment target and showed
no document-level horizontal overflow. Expanded the technical reference and
verified a direct fragment navigation reveals its nested identity heading.
Screenshots were visually inspected, including the mobile setup section.

Previews are in `/tmp/antiphon-readme-preview.17aXKu/`. This is a local GitHub-
style rendering check, not a claim that the unpublished commit was viewed on
github.com or npmjs.com. The cover uses no scripts, external fonts, raster
images or external SVG resources. Its text is also represented in the README
and image alt text. The `repository` field remains the public GitHub repo;
the final source push must precede publication so npm can resolve repository-
relative README links against the updated source.

## New package evidence

Artifact: `/tmp/antiphon-1.0.0-readme-pack.j86iSV/antiphon-1.0.0.tgz`

SHA-256:
`59a39281e0c4e5fc4db3b7f97d70d601403b4114ca8ae313e880abd6b2783866`

The package has 13 first-party files: the previous 12 plus the 4,126-byte SVG.
Compared with the `ef85f62` tarball, only README.md and package.json change;
the other 10 existing members have identical content and modes. No test,
preview dependency or review record is included.

The actual automated `test/ci/installed_tarball_smoke.py` accepted its checksum,
installed it into a separate temporary prefix, verified all 13 first-party
files, then passed version/help, refusal-before-host-spawn, generated setup,
Python import ownership and both MCP handshakes. Version remains 1.0.0.

Independent review is requested for this exact committed slice before Claude's
integration/publication workflow resumes. The previous runtime E2E evidence
belongs to its original exact SHA; executable bytes are unchanged here.
